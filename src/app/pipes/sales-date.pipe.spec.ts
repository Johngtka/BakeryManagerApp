import { SalesDatePipe } from './sales-date.pipe';

describe('SalesDatePipe', () => {
    it('create an instance', () => {
        const pipe = new SalesDatePipe();
        expect(pipe).toBeTruthy();
    });
});
