import { UpdateDescPipe } from './update-desc.pipe';

describe('UpdateDescPipe', () => {
    it('create an instance', () => {
        const pipe = new UpdateDescPipe();
        expect(pipe).toBeTruthy();
    });
});
